# Java Masterclass 2025


1. Запускается `public class HelloWorld {` из IDE

3. Запускается тесты `class HelloWorldTest {` из IDE

3. Проверка запуска тестов из командной строки: 
```
./gradlew test
```
Должно быть: 
```angular2html
BUILD SUCCESSFUL in ...ms
```